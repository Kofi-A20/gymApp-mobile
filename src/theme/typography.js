export const TYPOGRAPHY = {
  // Large screen hero title (e.g. "CALORIE COMMAND", "SESSION HISTORY.")
  heroTitle: {
    fontSize: 42,
    fontWeight: '900',
    letterSpacing: -2,
    lineHeight: 40,
  },

  // Small eyebrow label printed above the hero (e.g. "HEALTH DIAGNOSTICS")
  eyebrow: {
    fontSize: 10,
    fontWeight: '800',
    letterSpacing: 1.5,
  },

  // Section divider labels within a screen (e.g. "BIOMETRICS", "ACCOUNT DETAILS")
  sectionHeader: {
    fontSize: 12,
    fontWeight: '900',
    letterSpacing: 1.5,
  },

  // Small label at the top of a card or tile (e.g. "MACRO RATIOS")
  cardHeader: {
    fontSize: 10,
    fontWeight: '900',
    letterSpacing: 2,
  },

  // Text inside a horizontal tab bar (e.g. "DASHBOARD", "ANALYTICS")
  tabLabel: {
    fontSize: 12,
    fontWeight: '900',
    letterSpacing: 2,
  },

  // Label printed above a form input field
  fieldLabel: {
    fontSize: 9,
    fontWeight: '800',
    letterSpacing: 1,
  },

  // Standard body text
  body: {
    fontSize: 14,
    fontWeight: '600',
  },

  // Smallest label — units, secondary stats, badge text
  micro: {
    fontSize: 9,
    fontWeight: '800',
    letterSpacing: 1,
  },
};
